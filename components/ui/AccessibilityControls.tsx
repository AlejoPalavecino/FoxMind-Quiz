
// This component is obsolete and has been replaced by the global ConfigModal.
// It is kept to avoid breaking imports but should be removed in a future cleanup.
const AccessibilityControls: React.FC = () => {
  return null;
};

export default AccessibilityControls;